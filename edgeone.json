{
    "name": "another-graph-editor",
    "buildCommand": "pnpm run build",
    "installCommand": "pnpm install",
    "outputDirectory": "./dist",
    "rewrites": [
        {
            "source": "/assets/*",
            "destination": "/another_graph_editor/assets/:splat"
        },
        {
            "source": "/",
            "destination": "/index.html"
        }
    ]
}
