{
    "name": "another-graph-editor",
    "buildCommand": "pnpm run build && find dist -type f -exec sed -i 's|/another_graph_editor||g' {} +",
    "installCommand": "pnpm install",
    "outputDirectory": "./dist"
}
